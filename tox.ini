# content of: tox.ini , put in same dir as setup.py
[tox]
#envlist = py3{7,8,9,10,11,12}-boto3-{33,30,28,28,27,26}
envlist = py3{7}-boto3_{33,26}}
allowlist_externals = echo, ls

[testenv]
# install pytest in the virtualenv where commands will be executed
deps = 
    deepdiff
    boto3_33: boto3>=33.,<34
    boto3_30: boto3>=30.,<31
    boto3_28: boto3>=28.,<29
    boto3_27: boto3>=27.,<28
    boto3_26: boto3>=26.,<27

description = 
    boto3_33: the 33 yes
    boto3_26: twenty six

commands =
    # NOTE: you can run any command line tool here - not just tests
    pip freeze
    python -c "print('{[testenv]deps}')"
    python -m unittests.short_tests
